version: '3.8'

services:
  tour-management:
    build: .
    ports:
      - "8080:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - database_data:/Database
    restart: unless-stopped

volumes:
  database_data:
